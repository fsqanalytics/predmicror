% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_models.R
\name{fit_inactivation}
\alias{fit_inactivation}
\title{Fit a microbial inactivation model}
\usage{
fit_inactivation(data, model, time, response = "logN", start, ...)
}
\arguments{
\item{data}{A data frame containing the time and response variables.}

\item{model}{A model name, either quoted or unquoted. See
\code{\link[=predmicror_models]{predmicror_models()}}.}

\item{time}{Column containing time values, either quoted or unquoted.}

\item{response}{Column containing the response values, either quoted or
unquoted. Defaults to \code{"logN"}.}

\item{start}{Named list of initial parameter values for the selected model.}

\item{...}{Additional arguments passed to \code{\link[gslnls:gsl_nls]{gslnls::gsl_nls()}}.}
}
\value{
A \code{predmicror_fit} object with the fitted model and metadata.
}
\description{
\code{fit_inactivation()} validates the input data, builds the nonlinear model
formula, fits it with \code{\link[gslnls:gsl_nls]{gslnls::gsl_nls()}}, and returns a \code{predmicror_fit}
object.
}
\details{
Inactivation models expect the response to be the base 10 logarithm of the
microbial concentration, usually \code{logN}.
}
\examples{
data(mafart2005Li11)
fit <- fit_inactivation(
  mafart2005Li11,
  model = "WeibullM",
  time = "Time",
  response = "logN",
  start = list(Y0 = 10, sigma = 3, alpha = 1)
)
coef(fit)
}
