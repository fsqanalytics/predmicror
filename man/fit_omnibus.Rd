% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omnibus.R
\name{fit_omnibus}
\alias{fit_omnibus}
\alias{fit_omnibus_growth}
\alias{fit_omnibus_inactivation}
\title{Fit omnibus predictive microbiology models}
\usage{
fit_omnibus(
  data,
  type = c("growth", "inactivation"),
  primary,
  time,
  response,
  group,
  secondary = NULL,
  random,
  correlation = NULL,
  start,
  method = "ML",
  control = NULL,
  ...
)

fit_omnibus_growth(
  data,
  primary,
  time,
  response,
  group,
  secondary = NULL,
  random,
  correlation = NULL,
  start,
  method = "ML",
  control = NULL,
  ...
)

fit_omnibus_inactivation(
  data,
  primary,
  time,
  response,
  group,
  secondary = NULL,
  random,
  correlation = NULL,
  start,
  method = "ML",
  control = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{type}{Character string. One of \code{"growth"} or \code{"inactivation"}.}

\item{primary}{Character string naming a primary model registered in
\code{predmicror}, for example \code{"HuangNLM"} or \code{"WeibullM"}.}

\item{time, response, group}{Column names for time, response, and grouping
variable.}

\item{secondary}{Optional named list of formulas, one per primary-model
parameter. Parameters not listed are modelled as intercept-only effects.}

\item{random}{Random-effects formula passed to \code{\link[nlme:nlme]{nlme::nlme()}}. If the formula
does not include a grouping term, \verb{| group} is added automatically.}

\item{correlation}{Optional correlation structure. Use \code{"AR1"} for
\code{\link[nlme:corAR1]{nlme::corAR1()}} within groups, \code{NULL} for none, or pass an \code{nlme}
correlation object.}

\item{start}{Numeric vector of starting values for fixed effects, in the order
expected by \code{\link[nlme:nlme]{nlme::nlme()}}.}

\item{method}{Estimation method passed to \code{\link[nlme:nlme]{nlme::nlme()}}.}

\item{control}{Optional \code{\link[nlme:nlmeControl]{nlme::nlmeControl()}} object.}

\item{...}{Additional arguments passed to \code{\link[nlme:nlme]{nlme::nlme()}}.}
}
\value{
A \code{predmicror_omnibus_fit} object.
}
\description{
\code{fit_omnibus()} fits a nonlinear mixed-effects model in which the primary
model is one of the parameterised \code{predmicror} primary models and one or more
primary-model parameters are described by secondary covariate formulas.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
dat <- data.frame(
  Condition = rep(1:3, each = 5),
  Time = rep(c(1, 2, 4, 6, 8), 3)
)
dat$logN <- WeibullM(dat$Time, Y0 = 7, sigma = 6, alpha = 1)
fit <- fit_omnibus_inactivation(
  dat,
  primary = "WeibullM",
  time = "Time",
  response = "logN",
  group = "Condition",
  random = Y0 ~ 1,
  start = c(Y0 = 7, sigma = 6, alpha = 1)
)
\dontshow{\}) # examplesIf}
}
