% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_diagnostics.R
\name{compare_models}
\alias{compare_models}
\title{Compare fitted predmicror models}
\usage{
compare_models(..., sort_by = c("AIC", "BIC", "RMSE", "MAE", "none"))
}
\arguments{
\item{...}{\code{predmicror_fit} objects, or a single list of \code{predmicror_fit}
objects.}

\item{sort_by}{Character string. One of \code{"AIC"}, \code{"BIC"}, \code{"RMSE"},
\code{"MAE"}, or \code{"none"}.}
}
\value{
A data frame with one row per fitted model.
}
\description{
\code{compare_models()} combines the output of \code{\link[=fit_metrics]{fit_metrics()}} for two or more
fitted models. It is useful when choosing between alternative primary growth,
inactivation, or cardinal models fitted to the same response scale.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
data(growthfull)
huang <- fit_growth(
  growthfull,
  model = "HuangFM",
  time = "Time",
  response = "lnN",
  start = list(Y0 = 0, Ymax = 22, MUmax = 1.7, lag = 5)
)
baranyi <- fit_growth(
  growthfull,
  model = "BaranyiFM",
  time = "Time",
  response = "lnN",
  start = list(Y0 = 0, Ymax = 22, MUmax = 1.7, lag = 5)
)
compare_models(huang = huang, baranyi = baranyi)
\dontshow{\}) # examplesIf}
}
