% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_sensitivity.R
\name{dynamic_sensitivity}
\alias{dynamic_sensitivity}
\title{Finite-difference sensitivity for dynamic predictions}
\usage{
dynamic_sensitivity(
  type = c("growth", "inactivation"),
  profile,
  start,
  parameters = NULL,
  relative_delta = 1e-06,
  times = NULL,
  ...
)
}
\arguments{
\item{type}{Character string. One of \code{"growth"} or \code{"inactivation"}.}

\item{profile}{A \code{\link[=dynamic_profile]{dynamic_profile()}} object or compatible data frame.}

\item{start}{Named list of parameter values passed to the dynamic prediction
function.}

\item{parameters}{Character vector of parameter names to perturb. Defaults to
all numeric scalar entries in \code{start}.}

\item{relative_delta}{Relative perturbation size.}

\item{times}{Optional output times.}

\item{...}{Additional arguments passed to \code{\link[=predict_dynamic_growth]{predict_dynamic_growth()}} or
\code{\link[=predict_dynamic_inactivation]{predict_dynamic_inactivation()}}.}
}
\value{
A data frame with time, parameter, prediction, sensitivity, and
scaled sensitivity.
}
\description{
\code{dynamic_sensitivity()} perturbs parameters in a dynamic prediction and
returns unscaled and scaled sensitivity coefficients. It is designed as a
lightweight diagnostic for sampling design and parameter identifiability.
}
\examples{
profile <- dynamic_profile(time = c(0, 5, 10), temperature = c(10, 15, 20))
sens <- dynamic_sensitivity(
  "growth",
  profile = profile,
  start = list(logN0 = 2, logNmax = 8, a = 0.08, Tmin = 7, lag = 1),
  times = seq(0, 10, by = 2),
  dt = 0.25
)
head(sens)
}
