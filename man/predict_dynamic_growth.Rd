% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_growth.R
\name{predict_dynamic_growth}
\alias{predict_dynamic_growth}
\title{Predict microbial growth under dynamic environmental conditions}
\usage{
predict_dynamic_growth(
  profile,
  model = "huang",
  secondary = "huang_sqrt",
  start,
  times = NULL,
  scale = c("log10", "ln"),
  dt = 0.01,
  method = "rk4",
  temperature = "temperature"
)
}
\arguments{
\item{profile}{A \code{\link[=dynamic_profile]{dynamic_profile()}} object or data frame with a \code{time} column
and an environmental variable, usually \code{temperature}.}

\item{model}{Character string. Currently only \code{"huang"} is supported.}

\item{secondary}{Character string defining the secondary model for the growth
rate. One of \code{"huang_sqrt"}, \code{"huang_full_sqrt"}, or \code{"constant"}.}

\item{start}{Named list of parameters. Use \code{logN0} and \code{logNmax} for base-10
input, or \code{Y0} and \code{Ymax} for natural-log input. For \code{secondary = "huang_sqrt"}, supply \code{a} and \code{Tmin}. For \code{secondary = "huang_full_sqrt"}, also supply \code{b} and \code{Tmax}. For \code{secondary = "constant"}, supply \code{MUmax}.}

\item{times}{Optional numeric vector of output times. If \code{NULL}, a regular
sequence covering the profile is created from \code{dt}.}

\item{scale}{Scale of the returned \code{response} column. One of \code{"log10"} or
\code{"ln"}.}

\item{dt}{Maximum integration step used by the internal RK4 solver.}

\item{method}{Solver method. Currently only \code{"rk4"} is implemented.}

\item{temperature}{Name of the temperature column in \code{profile}.}
}
\value{
A \code{predmicror_dynamic_prediction} data frame with time, prediction,
\code{lnN}, \code{logN}, temperature, and metadata attributes.
}
\description{
\code{predict_dynamic_growth()} solves a differential Huang-type growth model over
a time-varying environmental profile. It is intended for forward prediction
under dynamic temperature conditions and uses an internal fourth-order
Runge-Kutta solver.
}
\examples{
profile <- dynamic_profile(
  time = c(0, 5, 10, 15, 20),
  temperature = c(10, 4, 15, 15, 10)
)
pred <- predict_dynamic_growth(
  profile = profile,
  start = list(logN0 = 2, logNmax = 8.8, a = 0.0886, Tmin = 8.91, lag = 2),
  dt = 0.25
)
head(pred)
}
