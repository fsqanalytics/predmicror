% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_inactivation.R
\name{predict_dynamic_inactivation}
\alias{predict_dynamic_inactivation}
\title{Predict microbial inactivation under dynamic environmental conditions}
\usage{
predict_dynamic_inactivation(
  profile,
  model = "weibull_peleg",
  secondary = "constant",
  start,
  times = NULL,
  dt = 0.01,
  method = "rk4",
  temperature = "temperature"
)
}
\arguments{
\item{profile}{A \code{\link[=dynamic_profile]{dynamic_profile()}} object or data frame with a \code{time} column.}

\item{model}{Character string. Currently only \code{"weibull_peleg"} is supported.}

\item{secondary}{Character string defining the secondary model for the Peleg
rate parameter \code{b}. Use \code{"constant"} for constant conditions or \code{"z_value"}
for a log-linear temperature effect.}

\item{start}{Named list. Supply \code{logN0}, shape parameter \code{n}, and either \code{b}
for \code{secondary = "constant"} or \code{b_ref}, \code{T_ref}, and \code{z} for \code{secondary = "z_value"}.}

\item{times}{Optional numeric vector of output times.}

\item{dt}{Maximum integration step used by the internal RK4 solver.}

\item{method}{Solver method. Currently only \code{"rk4"} is implemented.}

\item{temperature}{Name of the temperature column in \code{profile}.}
}
\value{
A \code{predmicror_dynamic_prediction} data frame with time, \code{logN},
\code{log_survival}, temperature, and metadata attributes.
}
\description{
\code{predict_dynamic_inactivation()} solves a dynamic Weibull-Peleg type
inactivation model over a time-varying environmental profile. The output is
returned on the base-10 logarithmic scale.
}
\examples{
profile <- dynamic_profile(time = c(0, 10, 20), temperature = c(55, 58, 60))
pred <- predict_dynamic_inactivation(
  profile = profile,
  start = list(logN0 = 7, b_ref = 0.15, T_ref = 55, z = 8, n = 1.2),
  secondary = "z_value",
  dt = 0.25
)
head(pred)
}
