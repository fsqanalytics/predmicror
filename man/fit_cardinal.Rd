% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_models.R
\name{fit_cardinal}
\alias{fit_cardinal}
\title{Fit a cardinal parameter model}
\usage{
fit_cardinal(data, model, x, response = "sqrtGR", start, ...)
}
\arguments{
\item{data}{A data frame containing the environmental factor and response
variables.}

\item{model}{A model name, either quoted or unquoted. See
\code{\link[=predmicror_models]{predmicror_models()}}.}

\item{x}{Column containing the environmental factor values, either quoted or
unquoted.}

\item{response}{Column containing the response values, either quoted or
unquoted. Defaults to \code{"sqrtGR"}.}

\item{start}{Named list of initial parameter values for the selected model.}

\item{...}{Additional arguments passed to \code{\link[gslnls:gsl_nls]{gslnls::gsl_nls()}}.}
}
\value{
A \code{predmicror_fit} object with the fitted model and metadata.
}
\description{
\code{fit_cardinal()} validates the input data, builds the nonlinear model formula,
fits it with \code{\link[gslnls:gsl_nls]{gslnls::gsl_nls()}}, and returns a \code{predmicror_fit} object.
}
\details{
Cardinal models expect the response to be the square root of the growth rate,
usually \code{sqrtGR}.
}
\examples{
data(salmonella)
fit <- fit_cardinal(
  salmonella,
  model = "CMTI",
  x = "Temp",
  response = "sqrtGR",
  start = list(Tmax = 42, Tmin = 1, MUopt = 1, Topt = 37)
)
coef(fit)
}
