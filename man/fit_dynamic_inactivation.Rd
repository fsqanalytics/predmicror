% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_fit.R
\name{fit_dynamic_inactivation}
\alias{fit_dynamic_inactivation}
\title{Fit dynamic microbial inactivation models}
\usage{
fit_dynamic_inactivation(
  data,
  profile,
  time,
  response,
  start,
  estimate = NULL,
  fixed = NULL,
  lower = NULL,
  upper = NULL,
  model = "weibull_peleg",
  secondary = "constant",
  dt = 0.01,
  method = "rk4",
  temperature = "temperature",
  optimizer = "L-BFGS-B",
  control = list(),
  ...
)
}
\arguments{
\item{data}{Data frame with observed microbial counts.}

\item{profile}{A \code{\link[=dynamic_profile]{dynamic_profile()}} object or compatible data frame.}

\item{time, response}{Column names in \code{data} containing observation time and
microbial response.}

\item{start}{Named list of starting parameter values passed to
\code{\link[=predict_dynamic_growth]{predict_dynamic_growth()}}. Numeric scalar entries are candidates for
estimation.}

\item{estimate}{Character vector of parameter names to estimate. If \code{NULL},
all numeric scalar entries in \code{start} except those listed in \code{fixed} are
estimated.}

\item{fixed}{Optional named list of parameters to keep fixed during fitting.}

\item{lower, upper}{Optional named numeric vectors or lists with lower and
upper bounds for estimated parameters.}

\item{model, secondary, dt, method, temperature}{Arguments passed to
\code{\link[=predict_dynamic_inactivation]{predict_dynamic_inactivation()}}.}

\item{optimizer}{Optimization method passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{control}{Optional control list passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{...}{Additional arguments passed to \code{\link[=predict_dynamic_growth]{predict_dynamic_growth()}}.}
}
\value{
A \code{predmicror_dynamic_fit} object.
}
\description{
\code{fit_dynamic_inactivation()} estimates selected parameters of a dynamic
Weibull-Peleg inactivation model by minimizing the residual sum of squares
against observed data.
}
\examples{
profile <- dynamic_profile(time = c(0, 10), temperature = c(60, 60))
obs <- data.frame(time = c(0, 5, 10), logN = c(7, 6, 5))
fit <- fit_dynamic_inactivation(
  obs,
  profile = profile,
  time = "time",
  response = "logN",
  start = list(logN0 = 7, b = 0.15, n = 1),
  estimate = "b",
  dt = 0.25
)
coef(fit)
}
