% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_diagnostics.R
\name{predmicror_augment}
\alias{predmicror_augment}
\alias{predmicror_augment.default}
\alias{predmicror_augment.predmicror_fit}
\alias{as.data.frame.predmicror_fit}
\title{Extract fitted values and residuals from a predmicror fit}
\usage{
predmicror_augment(object, ...)

\method{predmicror_augment}{default}(object, ...)

\method{predmicror_augment}{predmicror_fit}(object, newdata = NULL, ...)

\method{as.data.frame}{predmicror_fit}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{object, x}{A \code{predmicror_fit} object.}

\item{...}{Additional arguments passed to \code{predict()}.}

\item{newdata}{Optional data frame used for prediction. If \code{NULL}, the data
stored in the fitted object are used.}

\item{row.names, optional}{Arguments required by the base \code{as.data.frame()}
generic. They are accepted for method compatibility.}
}
\value{
A data frame containing the original columns plus \code{.fitted}, \code{.resid}
when the response column is available, \code{.model}, and \code{.type}.
}
\description{
\code{predmicror_augment()} returns the original data, or optional new data, with
columns containing fitted values and residuals. It is intentionally lightweight
and does not require the \code{broom} package.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
data(growthfull)
fit <- fit_growth(
  data = growthfull,
  model = "HuangFM",
  time = "Time",
  response = "lnN",
  start = list(Y0 = 0, Ymax = 22, MUmax = 1.7, lag = 5)
)
head(predmicror_augment(fit))
\dontshow{\}) # examplesIf}
}
