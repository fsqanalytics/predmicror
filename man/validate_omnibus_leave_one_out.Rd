% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omnibus.R
\name{validate_omnibus_leave_one_out}
\alias{validate_omnibus_leave_one_out}
\title{Validate an omnibus fit by leaving out one group}
\usage{
validate_omnibus_leave_one_out(object, group_value, level = 0, ...)
}
\arguments{
\item{object}{A \code{predmicror_omnibus_fit} object.}

\item{group_value}{Group value to leave out.}

\item{level}{Prediction level. Defaults to \code{0} for population-level
prediction of the left-out group.}

\item{...}{Additional arguments passed to \code{fit_omnibus()} during refitting.}
}
\value{
A list with the refitted model, validation data, predictions,
residuals, bias factor, and accuracy factor.
}
\description{
Validate an omnibus fit by leaving out one group
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
set.seed(1)
dat <- do.call(rbind, lapply(1:4, function(g) {
  Time <- c(1, 2, 4, 6, 8, 10)
  sigma <- 5 + 0.4 * g
  data.frame(
    Condition = g,
    Time = Time,
    Temp = 55 + g,
    logN = WeibullM(Time, Y0 = 7, sigma = sigma, alpha = 1.1) +
      rnorm(length(Time), 0, 0.03)
  )
}))
fit <- fit_omnibus_inactivation(
  dat, primary = "WeibullM", time = "Time", response = "logN",
  group = "Condition", secondary = list(sigma = ~ Temp), random = Y0 ~ 1,
  start = c(Y0 = 7, sigma = 1, sigma.Temp = 0.08, alpha = 1)
)
loo <- validate_omnibus_leave_one_out(fit, group_value = 1)
loo$bias_factor
loo$accuracy_factor
\dontshow{\}) # examplesIf}
}
