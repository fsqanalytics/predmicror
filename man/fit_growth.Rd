% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_models.R
\name{fit_growth}
\alias{fit_growth}
\title{Fit a primary growth model}
\usage{
fit_growth(data, model, time, response = "lnN", start, ...)
}
\arguments{
\item{data}{A data frame containing the time and response variables.}

\item{model}{A model name, either quoted or unquoted. See
\code{\link[=predmicror_models]{predmicror_models()}}.}

\item{time}{Column containing time values, either quoted or unquoted.}

\item{response}{Column containing the response values, either quoted or
unquoted. Defaults to \code{"lnN"}.}

\item{start}{Named list of initial parameter values for the selected model.}

\item{...}{Additional arguments passed to \code{\link[gslnls:gsl_nls]{gslnls::gsl_nls()}}.}
}
\value{
A \code{predmicror_fit} object with the fitted model and metadata.
}
\description{
\code{fit_growth()} validates the input data, builds the nonlinear model formula,
fits it with \code{\link[gslnls:gsl_nls]{gslnls::gsl_nls()}}, and returns a \code{predmicror_fit} object.
}
\details{
Growth models expect the response to be the natural logarithm of the microbial
concentration, usually \code{lnN}.
}
\examples{
data(growthfull)
fit <- fit_growth(
  growthfull,
  model = "HuangFM",
  time = "Time",
  response = "lnN",
  start = list(Y0 = 0, Ymax = 22, MUmax = 1.7, lag = 5)
)
coef(fit)
}
